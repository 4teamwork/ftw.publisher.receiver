Introduction
============

The package ftw.publisher.receiver is used for receiving objects form a instance with ftw.publisher.sender installed and creating or deleting objects according to the data received from ftw.publisher.sender
